---
phase: 4
plan: 01
title: "Dashboard Card Components"
wave: 1
depends_on: []
requirements: ["DASH-01", "DASH-02", "DASH-03", "DASH-06"]
files_modified:
  - "src/components/dashboard/RiskCard.tsx"
  - "src/components/dashboard/ProductCard.tsx"
  - "src/components/dashboard/PartnerCard.tsx"
  - "src/components/dashboard/ResourceList.tsx"
  - "src/components/dashboard/AdvisorCTA.tsx"
  - "src/lib/types.ts"
  - "src/lib/schemas.ts"
  - "src/config/partners.ts"
  - "tests/dashboard-cards.test.ts"
autonomous: true
---

# Plan 01: Dashboard Card Components

<objective>
Build the four dashboard card components (RiskCard, ProductCard, PartnerCard, ResourceList) plus the AdvisorCTA, backed by Zod-validated TypeScript types and a static partner config. These components are pure presentational -- they receive props and render. No data fetching, no animation (that comes in Plans 03-04).
</objective>

<must_haves>
- RiskCard renders severity badge with color coding (high=red, medium=amber, low=green) and Lucide icons
- ProductCard renders product name, relevance explanation, coverage type badge, and optional external link
- PartnerCard renders Caarl, Doado, Noctia with distinct icons, taglines, and relevance text
- ResourceList renders curated resource links with type badges (article/guide/tool/faq)
- AdvisorCTA renders a "Contacter un conseiller MetLife" button with Phone icon
- Zod schemas validate all dashboard data structures before rendering
- All visible text is in French
</must_haves>

## Tasks

### Task 1: Install Dependencies (motion, zod)

<read_first>
- /Users/paulduchateau/projects/METLife/package.json
- /Users/paulduchateau/projects/METLife/.planning/research/STACK.md (Recommended Stack section)
</read_first>

<action>
Run from `/Users/paulduchateau/projects/METLife/`:

```bash
npm install motion zod
```

Also install the shadcn Badge component:

```bash
npx shadcn@latest add badge
```

Motion v12.37.x and Zod v4.3.x will be used across all Phase 4 plans. Badge is needed for severity and type indicators in cards.
</action>

<acceptance_criteria>
- `grep '"motion"' package.json` returns a match
- `grep '"zod"' package.json` returns a match
- `src/components/ui/badge.tsx` exists
</acceptance_criteria>

### Task 2: Define Dashboard TypeScript Types and Zod Schemas

<read_first>
- /Users/paulduchateau/projects/METLife/.planning/phases/04-dashboard-transition/04-RESEARCH.md (Section 5.3 Zod Validation)
- /Users/paulduchateau/projects/METLife/.planning/research/ARCHITECTURE.md (Key Data Types section)
- /Users/paulduchateau/projects/METLife/src/lib/utils.ts
</read_first>

<action>
Create `src/lib/types.ts` with the dashboard data interfaces:

```typescript
export interface Risk {
  id: string;
  label: string;
  severity: "high" | "medium" | "low";
  description: string;
}

export interface ProductRecommendation {
  id: string;
  name: string;
  relevance: string;
  url?: string;
  coverageType?: string;
}

export interface PartnerRecommendation {
  id: "caarl" | "doado" | "noctia";
  relevance: string;
}

export interface Resource {
  title: string;
  url: string;
  type: "article" | "guide" | "tool" | "faq";
}

export interface ProspectProfile {
  profession: string;
  sector: string;
  concerns: string[];
}

export interface DashboardData {
  risks: Risk[];
  products: ProductRecommendation[];
  partners: PartnerRecommendation[];
  resources: Resource[];
  profile: ProspectProfile;
}

export interface ChatMessage {
  id: string;
  role: "user" | "assistant";
  content: string;
  createdAt: Date;
}
```

Create `src/lib/schemas.ts` with Zod validation schemas:

```typescript
import { z } from "zod";

export const riskSchema = z.object({
  id: z.string(),
  label: z.string(),
  severity: z.enum(["high", "medium", "low"]),
  description: z.string(),
});

export const productSchema = z.object({
  id: z.string(),
  name: z.string(),
  relevance: z.string(),
  url: z.string().optional(),
  coverageType: z.string().optional(),
});

export const partnerSchema = z.object({
  id: z.enum(["caarl", "doado", "noctia"]),
  relevance: z.string(),
});

export const resourceSchema = z.object({
  title: z.string(),
  url: z.string(),
  type: z.enum(["article", "guide", "tool", "faq"]),
});

export const profileSchema = z.object({
  profession: z.string(),
  sector: z.string(),
  concerns: z.array(z.string()),
});

export const dashboardDataSchema = z.object({
  risks: z.array(riskSchema),
  products: z.array(productSchema),
  partners: z.array(partnerSchema).optional().default([]),
  resources: z.array(resourceSchema).optional().default([]),
  profile: profileSchema,
});

export type DashboardDataInput = z.input<typeof dashboardDataSchema>;
export type DashboardDataOutput = z.output<typeof dashboardDataSchema>;
```
</action>

<acceptance_criteria>
- `src/lib/types.ts` contains `export interface Risk`
- `src/lib/types.ts` contains `export interface DashboardData`
- `src/lib/types.ts` contains `severity: "high" | "medium" | "low"`
- `src/lib/schemas.ts` contains `dashboardDataSchema`
- `src/lib/schemas.ts` contains `z.enum(["caarl", "doado", "noctia"])`
- `src/lib/schemas.ts` contains `z.enum(["article", "guide", "tool", "faq"])`
</acceptance_criteria>

### Task 3: Create Partner Configuration

<read_first>
- /Users/paulduchateau/projects/METLife/.planning/research/ARCHITECTURE.md (Partner Services section)
- /Users/paulduchateau/projects/METLife/.planning/phases/04-dashboard-transition/04-RESEARCH.md (Section 3.2 Partner Card)
</read_first>

<action>
Create directory `src/config/` and file `src/config/partners.ts`:

```typescript
export const partners = {
  caarl: {
    name: "Caarl",
    tagline: "Assistance juridique pour TNS",
    description:
      "Protection juridique et accompagnement administratif pour les travailleurs non salaries.",
    icon: "Scale" as const,
    url: "#",
  },
  doado: {
    name: "Doado",
    tagline: "Prevention des TMS",
    description:
      "Programme personnalise de prevention des troubles musculo-squelettiques adapte a votre activite.",
    icon: "Activity" as const,
    url: "#",
  },
  noctia: {
    name: "Noctia",
    tagline: "Gestion du sommeil",
    description:
      "Diagnostic et accompagnement pour ameliorer la qualite du sommeil et reduire le stress.",
    icon: "Moon" as const,
    url: "#",
  },
} as const;

export type PartnerId = keyof typeof partners;
```
</action>

<acceptance_criteria>
- `src/config/partners.ts` contains `caarl`
- `src/config/partners.ts` contains `doado`
- `src/config/partners.ts` contains `noctia`
- `src/config/partners.ts` contains `Assistance juridique`
- `src/config/partners.ts` contains `Prevention des TMS`
- `src/config/partners.ts` contains `Gestion du sommeil`
</acceptance_criteria>

### Task 4: Create RiskCard Component

<read_first>
- /Users/paulduchateau/projects/METLife/.planning/phases/04-dashboard-transition/04-RESEARCH.md (Section 3.2 RiskCard)
- /Users/paulduchateau/projects/METLife/src/components/ui/card.tsx
- /Users/paulduchateau/projects/METLife/src/components/ui/badge.tsx (after Task 1)
</read_first>

<action>
Create directory `src/components/dashboard/` and file `src/components/dashboard/RiskCard.tsx`:

```tsx
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
} from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { ShieldAlert, Shield, ShieldCheck } from "lucide-react";
import type { Risk } from "@/lib/types";

const severityConfig = {
  high: {
    color: "bg-red-100 text-red-800 border-red-200",
    label: "Risque eleve",
    Icon: ShieldAlert,
    borderColor: "#ef4444",
  },
  medium: {
    color: "bg-amber-100 text-amber-800 border-amber-200",
    label: "Risque modere",
    Icon: Shield,
    borderColor: "#f59e0b",
  },
  low: {
    color: "bg-green-100 text-green-800 border-green-200",
    label: "Risque faible",
    Icon: ShieldCheck,
    borderColor: "#22c55e",
  },
};

export function RiskCard({ risk }: { risk: Risk }) {
  const config = severityConfig[risk.severity];

  return (
    <Card
      data-testid="risk-card"
      className="border-l-4"
      style={{ borderLeftColor: config.borderColor }}
    >
      <CardHeader>
        <div className="flex items-center justify-between">
          <div className="flex items-center gap-2">
            <config.Icon className="h-5 w-5" />
            <CardTitle className="text-base">{risk.label}</CardTitle>
          </div>
          <Badge variant="outline" className={config.color}>
            {config.label}
          </Badge>
        </div>
      </CardHeader>
      <CardContent>
        <CardDescription className="text-sm leading-relaxed">
          {risk.description}
        </CardDescription>
      </CardContent>
    </Card>
  );
}
```
</action>

<acceptance_criteria>
- `src/components/dashboard/RiskCard.tsx` contains `export function RiskCard`
- `src/components/dashboard/RiskCard.tsx` contains `data-testid="risk-card"`
- `src/components/dashboard/RiskCard.tsx` contains `severityConfig`
- `src/components/dashboard/RiskCard.tsx` contains `ShieldAlert`
- `src/components/dashboard/RiskCard.tsx` contains `Risque eleve`
- `src/components/dashboard/RiskCard.tsx` contains `Risque modere`
- `src/components/dashboard/RiskCard.tsx` contains `Risque faible`
</acceptance_criteria>

### Task 5: Create ProductCard Component

<read_first>
- /Users/paulduchateau/projects/METLife/.planning/phases/04-dashboard-transition/04-RESEARCH.md (Section 3.2 ProductCard)
- /Users/paulduchateau/projects/METLife/src/components/ui/card.tsx
- /Users/paulduchateau/projects/METLife/src/components/ui/button.tsx
</read_first>

<action>
Create `src/components/dashboard/ProductCard.tsx`:

```tsx
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { ExternalLink } from "lucide-react";
import type { ProductRecommendation } from "@/lib/types";

export function ProductCard({ product }: { product: ProductRecommendation }) {
  return (
    <Card data-testid="product-card">
      <CardHeader>
        <div className="flex items-center justify-between">
          <CardTitle className="text-base">{product.name}</CardTitle>
          {product.coverageType && (
            <Badge variant="secondary">{product.coverageType}</Badge>
          )}
        </div>
      </CardHeader>
      <CardContent>
        <CardDescription className="text-sm leading-relaxed">
          {product.relevance}
        </CardDescription>
      </CardContent>
      {product.url && (
        <CardFooter>
          <Button variant="outline" size="sm" asChild>
            <a href={product.url} target="_blank" rel="noopener noreferrer">
              En savoir plus <ExternalLink className="ml-1 h-3 w-3" />
            </a>
          </Button>
        </CardFooter>
      )}
    </Card>
  );
}
```
</action>

<acceptance_criteria>
- `src/components/dashboard/ProductCard.tsx` contains `export function ProductCard`
- `src/components/dashboard/ProductCard.tsx` contains `data-testid="product-card"`
- `src/components/dashboard/ProductCard.tsx` contains `En savoir plus`
- `src/components/dashboard/ProductCard.tsx` contains `ExternalLink`
- `src/components/dashboard/ProductCard.tsx` contains `target="_blank"`
</acceptance_criteria>

### Task 6: Create PartnerCard Component

<read_first>
- /Users/paulduchateau/projects/METLife/.planning/phases/04-dashboard-transition/04-RESEARCH.md (Section 3.2 PartnerCard)
- /Users/paulduchateau/projects/METLife/src/config/partners.ts (after Task 3)
</read_first>

<action>
Create `src/components/dashboard/PartnerCard.tsx`:

```tsx
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
} from "@/components/ui/card";
import { Scale, Activity, Moon } from "lucide-react";
import { partners } from "@/config/partners";
import type { PartnerRecommendation } from "@/lib/types";

const partnerIcons = {
  caarl: Scale,
  doado: Activity,
  noctia: Moon,
} as const;

export function PartnerCard({ partner }: { partner: PartnerRecommendation }) {
  const config = partners[partner.id];
  const Icon = partnerIcons[partner.id];

  return (
    <Card data-testid="partner-card" className="bg-muted/50">
      <CardHeader>
        <div className="flex items-center gap-3">
          <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-primary/10">
            <Icon className="h-5 w-5 text-primary" />
          </div>
          <div>
            <CardTitle className="text-base">{config.name}</CardTitle>
            <CardDescription className="text-xs">
              {config.tagline}
            </CardDescription>
          </div>
        </div>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">{partner.relevance}</p>
      </CardContent>
    </Card>
  );
}
```
</action>

<acceptance_criteria>
- `src/components/dashboard/PartnerCard.tsx` contains `export function PartnerCard`
- `src/components/dashboard/PartnerCard.tsx` contains `data-testid="partner-card"`
- `src/components/dashboard/PartnerCard.tsx` contains `partners[partner.id]`
- `src/components/dashboard/PartnerCard.tsx` contains `partnerIcons`
- `src/components/dashboard/PartnerCard.tsx` contains `Scale, Activity, Moon`
</acceptance_criteria>

### Task 7: Create ResourceList Component

<read_first>
- /Users/paulduchateau/projects/METLife/.planning/phases/04-dashboard-transition/04-RESEARCH.md (Section 3.1 Card Types)
</read_first>

<action>
Create `src/components/dashboard/ResourceList.tsx`:

```tsx
import { Badge } from "@/components/ui/badge";
import { ExternalLink, FileText, BookOpen, Wrench, HelpCircle } from "lucide-react";
import type { Resource } from "@/lib/types";

const typeConfig = {
  article: { label: "Article", Icon: FileText },
  guide: { label: "Guide", Icon: BookOpen },
  tool: { label: "Outil", Icon: Wrench },
  faq: { label: "FAQ", Icon: HelpCircle },
} as const;

export function ResourceList({ resources }: { resources: Resource[] }) {
  if (resources.length === 0) return null;

  return (
    <ul data-testid="resource-list" className="space-y-3">
      {resources.map((resource, index) => {
        const config = typeConfig[resource.type];
        return (
          <li key={`${resource.url}-${index}`}>
            <a
              href={resource.url}
              target="_blank"
              rel="noopener noreferrer"
              className="flex items-center gap-3 rounded-lg border border-border p-3 transition-colors hover:bg-muted"
            >
              <config.Icon className="h-4 w-4 text-muted-foreground shrink-0" />
              <span className="flex-1 text-sm font-medium">
                {resource.title}
              </span>
              <Badge variant="outline" className="text-xs shrink-0">
                {config.label}
              </Badge>
              <ExternalLink className="h-3 w-3 text-muted-foreground shrink-0" />
            </a>
          </li>
        );
      })}
    </ul>
  );
}
```
</action>

<acceptance_criteria>
- `src/components/dashboard/ResourceList.tsx` contains `export function ResourceList`
- `src/components/dashboard/ResourceList.tsx` contains `data-testid="resource-list"`
- `src/components/dashboard/ResourceList.tsx` contains `typeConfig`
- `src/components/dashboard/ResourceList.tsx` contains `Article`
- `src/components/dashboard/ResourceList.tsx` contains `resources.length === 0`
</acceptance_criteria>

### Task 8: Create AdvisorCTA Component

<read_first>
- /Users/paulduchateau/projects/METLife/.planning/phases/04-dashboard-transition/04-RESEARCH.md (Section 7 Advisor CTA)
</read_first>

<action>
Create `src/components/dashboard/AdvisorCTA.tsx`:

```tsx
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Phone } from "lucide-react";

export function AdvisorCTA() {
  return (
    <Card
      data-testid="advisor-cta"
      className="sticky bottom-4 border-primary/20 bg-primary/5"
    >
      <CardContent className="flex items-center justify-between py-4">
        <div>
          <p className="font-semibold text-sm">
            Vous souhaitez aller plus loin ?
          </p>
          <p className="text-xs text-muted-foreground">
            Un conseiller MetLife peut vous accompagner
          </p>
        </div>
        <Button className="bg-primary hover:bg-primary-dark">
          <Phone className="mr-2 h-4 w-4" />
          Contacter un conseiller
        </Button>
      </CardContent>
    </Card>
  );
}
```
</action>

<acceptance_criteria>
- `src/components/dashboard/AdvisorCTA.tsx` contains `export function AdvisorCTA`
- `src/components/dashboard/AdvisorCTA.tsx` contains `data-testid="advisor-cta"`
- `src/components/dashboard/AdvisorCTA.tsx` contains `Contacter un conseiller`
- `src/components/dashboard/AdvisorCTA.tsx` contains `Phone`
- `src/components/dashboard/AdvisorCTA.tsx` contains `sticky bottom-4`
</acceptance_criteria>

### Task 9: Write Unit Tests for Card Components and Schemas

<read_first>
- /Users/paulduchateau/projects/METLife/vitest.config.ts
- /Users/paulduchateau/projects/METLife/src/lib/schemas.ts (after Task 2)
</read_first>

<action>
Install React Testing Library:

```bash
npm install -D @testing-library/react @testing-library/jest-dom jsdom
```

Update `vitest.config.ts` to add jsdom environment:

```typescript
import { defineConfig } from "vitest/config";
import path from "path";

export default defineConfig({
  test: {
    include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],
    environment: "jsdom",
    coverage: {
      provider: "v8",
    },
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
});
```

Create `tests/dashboard-cards.test.ts`:

```typescript
import { describe, it, expect } from "vitest";
import { dashboardDataSchema } from "@/lib/schemas";

const validDashboardData = {
  risks: [
    {
      id: "risk-1",
      label: "Arret de travail prolonge",
      severity: "high" as const,
      description: "En tant que kine liberal, un arret de travail met en peril vos revenus.",
    },
  ],
  products: [
    {
      id: "prod-1",
      name: "MetLife Super Novaterm",
      relevance: "Couvre votre incapacite de travail avec des indemnites journalieres.",
      url: "https://www.metlife.fr/prevoyance-tns",
      coverageType: "prevoyance",
    },
  ],
  partners: [
    { id: "doado" as const, relevance: "Prevention TMS adaptee aux kinesitherapeutes." },
  ],
  resources: [
    { title: "Guide prevoyance TNS", url: "https://metlife.fr/guide", type: "guide" as const },
  ],
  profile: {
    profession: "Kinesitherapeute",
    sector: "Sante",
    concerns: ["incapacite", "prevoyance"],
  },
};

describe("Dashboard Zod Schemas", () => {
  it("validates a complete dashboard data object", () => {
    const result = dashboardDataSchema.safeParse(validDashboardData);
    expect(result.success).toBe(true);
  });

  it("rejects invalid severity values", () => {
    const invalid = {
      ...validDashboardData,
      risks: [{ ...validDashboardData.risks[0], severity: "critical" }],
    };
    const result = dashboardDataSchema.safeParse(invalid);
    expect(result.success).toBe(false);
  });

  it("rejects invalid partner IDs", () => {
    const invalid = {
      ...validDashboardData,
      partners: [{ id: "unknown", relevance: "test" }],
    };
    const result = dashboardDataSchema.safeParse(invalid);
    expect(result.success).toBe(false);
  });

  it("defaults partners and resources to empty arrays when omitted", () => {
    const minimal = {
      risks: validDashboardData.risks,
      products: validDashboardData.products,
      profile: validDashboardData.profile,
    };
    const result = dashboardDataSchema.safeParse(minimal);
    expect(result.success).toBe(true);
    if (result.success) {
      expect(result.data.partners).toEqual([]);
      expect(result.data.resources).toEqual([]);
    }
  });

  it("rejects missing required fields", () => {
    const result = dashboardDataSchema.safeParse({});
    expect(result.success).toBe(false);
  });
});
```
</action>

<acceptance_criteria>
- `tests/dashboard-cards.test.ts` contains `describe("Dashboard Zod Schemas"`
- `tests/dashboard-cards.test.ts` contains `dashboardDataSchema.safeParse`
- `tests/dashboard-cards.test.ts` contains `rejects invalid severity`
- `tests/dashboard-cards.test.ts` contains `rejects invalid partner IDs`
- `npx vitest run tests/dashboard-cards.test.ts` passes all 5 tests
</acceptance_criteria>

## Verification

<verification>
1. Run `npx vitest run tests/dashboard-cards.test.ts --reporter=verbose` -- all 5 schema tests pass
2. Confirm all card component files exist in `src/components/dashboard/`:
   - `RiskCard.tsx`, `ProductCard.tsx`, `PartnerCard.tsx`, `ResourceList.tsx`, `AdvisorCTA.tsx`
3. Run `npx tsc --noEmit` -- no TypeScript errors in the new files
4. Verify `src/lib/types.ts` exports `DashboardData`, `Risk`, `ProductRecommendation`, `PartnerRecommendation`, `Resource`
5. Verify `src/lib/schemas.ts` exports `dashboardDataSchema`
6. Verify `src/config/partners.ts` defines all three partners (caarl, doado, noctia)
</verification>
